# Documentação do Token Saver MCP

Site Docusaurus com os guias de uso e desenvolvimento do projeto.

```shell
npm install
npm run typecheck
npm run build
npm start
```

O conteúdo publicado fica em `docs/`; a ordem de navegação é definida em
`sidebars.ts`.
